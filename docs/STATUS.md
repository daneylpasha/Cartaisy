# Mobile Status

Last updated: 2026-08-06 (docs-only: added `FormInput`'s `selectionColor={colors.primary}` to the runtime-branding reactivity gap — `assets/colors.ts` re-exports `tokens.color` under a different name, and 34 `FormInput` instances across 16 production files read it through that alias, uncounted by every previously-listed bucket; synced across all four sections. Also this date: added `PrimaryButton`'s default `background="primary"` (passed straight into `OpTouch`'s static resolver) to the runtime-branding reactivity gap — 35 of ~38 production `PrimaryButton` instances rely on that default, spanning checkout, authentication, address, and product actions, and were entirely uncounted by every previously-listed bucket; synced across all four sections. Also this date: added `OpTouch`'s own static-token resolver to the runtime-branding reactivity gap — 3 merchant-visible buttons (`onboardingSlides.tsx`'s two nav arrows, `profile.tsx`'s "Sign in" button) pass `backgroundColor="primary"` through it and retain the bundled color, uncounted by every previously-listed bucket; synced across all four sections. Also this date: added 66 direct `tokens.color.primary`/`tokens.color.secondary` reads across 13 production files, plus `SecondaryButton`'s own static-token resolver (used by 19 other files), to the runtime-branding reactivity gap — these bypass `getTokenValue()` entirely and were omitted from the previously-recorded ~73-call-site count, understating the gap's true scope; synced across all four sections. Also this date: reconciled the branding/theme lines with shipped PRs #115/#116 — runtime `primaryColor`/`secondaryColor`/`logoUrl` are implemented, not "documented as a contract"; corrected an inaccurate "picks up on restart" claim about `getTokenValue()`/`AppImage` static-token reads and an understated "handful" call-site count (~73 call sites across ~39 files, not a handful); added the previously-undocumented-here 3-digit-hex rejection gap; synced the target-state and Current Priority Areas sections to match. Prior update 2026-07-28: added `docs/IOS_PROVISIONING_SCOPE.md` for roadmap Phase 2 item 3, and corrected the provisioning runbook's Step 3 iOS bullet, which still described Apple Developer Program ownership as an open decision after it was settled on 2026-07-17. Prior update 2026-07-17: GitHub issue #94 verified mobile CI green on a fresh PR and recorded the baseline; the documented Jest baseline was corrected from 78 to 79 tests to match the current tree. GitHub issue #89 cleaned EAS archive hygiene for generated local report artifacts and removed legacy public Shopify env-key guidance. GitHub issue #87 re-ran the mobile backend and checkout/orders smoke suites against the only available public backend candidate; no verified staging URL or safe Store A / Store B IDs were provided, and the local public config target still returned Railway platform `404 Application not found`. GitHub issue #86 created the real internal sample EAS project `@rendernext/acme-outfitters`, but the Android EAS development build still produced no artifact because remote Android credentials/keystore are not initialized and EAS cannot generate a new keystore in `--non-interactive` mode).

This file is a human/agent-maintained snapshot. It is not an automatically guaranteed source of truth. Verify current behavior in code before implementation work.

## Current State

Current state: The mobile app engine is an Expo/React Native app with routed shopping, auth, account, cart, checkout, home, catalog, notification, and profile surfaces.

Current state: The repo has useful mobile docs for environment variables, branded builds, per-merchant provisioning (`docs/MOBILE_MERCHANT_PROVISIONING_RUNBOOK.md`), branding/config boundaries, dynamic app config migration, home API usage, and runtime branding contracts.

Current state: `docs/IOS_PROVISIONING_SCOPE.md` (added 2026-07-28) is the planning reference for the iOS path, covering roadmap Phase 2 item 3 — Apple Developer Program enrollment requirements and lead time, App Store Connect setup, and TestFlight versus ad hoc distribution for getting a branded build onto a physical device. It is reference material only and triggers no enrollment or purchase. In the same change, the provisioning runbook's Step 3 was corrected: its iOS bullet described Apple Developer Program ownership as an open decision, contradicting the 2026-07-17 decision in the `cartaisy-backend` repo's `docs/DECISIONS.md` that each merchant enrolls in and owns their own Apple and Google Play accounts. Step 3 now states the merchant-owned position and cites that decision, while keeping the legitimate exception that a Cartaisy-controlled team remains acceptable for genuinely internal, non-merchant-branded development builds.

Current state: As a follow-on to that Step 3 fix, three further stale spots in `docs/MOBILE_MERCHANT_PROVISIONING_RUNBOOK.md` were corrected on 2026-07-28. Step 2's Expo account-ownership paragraph and the closing automation section's "blocked on decisions" claim now cite the 2026-07-23 decision that merchant EAS projects live under a Cartaisy-managed Expo organization, and Step 5's push-notification note no longer claims its automation is gated on the 2026-07-17 Apple account-ownership decision. No new decision was made and no automation was built — only status text that had fallen out of date with the recorded decisions.

Current state: Completing that lineage, two operational requirements from the 2026-07-23 decision that had never been written into the runbook they apply to were added on 2026-07-28. Step 2 now states that the Cartaisy Expo organization requires hardware-key 2FA and scoped organization tokens for automation, and Step 3 gains an "Offboarding — Handing Back What Cartaisy Holds" subsection: the Android upload keystore export, the bundle identifiers, what does not transfer because it was always merchant-owned, deletion of the EAS-held signing material once the merchant confirms receipt, transfer of Firebase ownership followed by removal of Cartaisy's IAM access, and revocation of Cartaisy's access to the merchant's Apple and Google Play accounts. The merchant-agreement half of offboarding is explicitly out of scope for the repo and remains an open business/legal task.

Current state: Public API base URL and store ID configuration are validated in `api/config/mobileConfig.ts`, and backend API calls include `X-Store-ID`.

Current state: Store config currently loads `currency`, `timezone`, and `name` from `/store/config` with local fallbacks.

Current state: `package.json` exposes `start`, `android`, `ios`, `web`, `lint`, `typecheck`, `test`, and `generate:api` scripts. `npm test` runs Jest (`jest-expo` preset via `jest.config.js`) with a current baseline of 15 test suites / 79 tests (verified 2026-07-17, GitHub issue #94; corrected from the previously documented 78 tests). Coverage includes real suites in `utils/__tests__/`, `api/config/__tests__/`, `api/__tests__/` (auth refresh), `store/__tests__/` (cart/auth stores), and focused component/screen tests for unavailable states, auth gating, cart checkout handoff, checkout beta gating, legacy address-list checkout returns, and home module resilience.

Current state: npm is the authoritative package manager. `package.json` declares `"packageManager": "npm@10.8.2"`, `package-lock.json` is the only committed lockfile, and `yarn.lock` was removed and gitignored. As of 2026-07-02, a clean checkout validates with `npm ci`, `npm run lint`, `npm run typecheck`, and `npm test`, matching CI.

## Readiness Snapshot

Current state: App engine readiness is partial. Core Expo Router, Tamagui, React Query, Zustand, Orval, Firebase messaging, and Stripe dependencies are present.

Current state: Store/tenant config is partial. `EXPO_PUBLIC_STORE_ID` and `X-Store-ID` behavior are present, but the store ID is public client configuration and backend tenant checks remain authoritative.

Current state: Tenant-mismatch verification is blocked on backend sandbox availability. A 2026-07-13 issue #87 rerun found no verified staging URL or safe Store A / Store B IDs in the issue body or comments, then re-tested the only available local public backend candidate with network access. The target still returned Railway `404 Application not found`, so correct-store, wrong-store, inactive-store, nonexistent/malformed-store, authenticated cross-store, guest-session cross-store, cart, checkout handoff, orders, product/search, and home behavior could not be freshly verified. The last backend-code run remains the historical local sandbox result in `docs/CROSS_REPO_SMOKE_TEST.md` and `docs/CHECKOUT_ORDERS_SMOKE_TEST.md`: `/store/config` enforced clean store validation, while `/customer/auth/profile`, `/customer/orders`, and `/unified-cart` still need backend tenant-mismatch hardening follow-up.

Current state: Branding/theme is implemented at both build time and runtime. App identity is generated at build time by dynamic `app.config.ts` from environment variables with cartaisy defaults. At runtime, `primaryColor`/`secondaryColor` apply live via Tamagui's `updateTheme` (no rebuild, PRs #115/#116) and `logoUrl` renders across 7 surfaces with a bundled build-time fallback; both have WCAG contrast guardrails. Known gaps: the hardcoded `t("common.companyName")` string (5 UI locations) isn't wired to dashboard branding; `getTokenValue("$primary"/"$secondary")` reads the static, bundled `tokens.color` object directly rather than Tamagui's theme registry, so it never picks up a merchant's runtime color (not "a handful" — a live grep on 2026-08-06 found roughly 43 `$primary` and 30 `$secondary` `getTokenValue()` call sites across ~39 files, covering backgrounds, gradients, and indicator colors, not just tints), and `AppImage.resolveTokenColor()`'s 4+ `tintColor` usages have the same static-read problem; separately, a live grep also found 66 further call sites across 13 production files (e.g. `personalInfo.tsx`'s date-picker UI, `CheckoutStepper.tsx`, `LoaderScreen.tsx`, `addNewCardDetails.tsx`) that read `tokens.color.primary`/`tokens.color.secondary` directly, bypassing `getTokenValue()` but hitting the same static object, and `SecondaryButton`'s own local token-resolution helper always reads `tokens.color` for its border/text color rather than the shared reactive theme, so none of the 19 other files rendering it pick up a runtime color change either; `OpTouch` (the shared touchable underlying most of the app's buttons) has the identical local-resolver pattern for `backgroundColor`/`borderColor`/`shadowColor`, and 3 merchant-visible buttons pass it `backgroundColor="primary"` directly (`onboardingSlides.tsx`'s two navigation arrows, `profile.tsx`'s "Sign in" prompt); further, `PrimaryButton` — the app's main CTA component — defaults its own `background` prop to `"primary"` and forwards it straight into `OpTouch`, so 35 of its ~38 production instances (checkout, authentication, address, and product actions) inherit the same static color, none of them tracked by any bucket above; `FormInput` reads the same static object through yet another alias — `assets/colors.ts` re-exports `tokens.color` under the name `colors`, and `FormInput`'s `selectionColor={colors.primary}` means all 34 of its instances across 16 production files (every text field in the app) retain the bundled color too; an app restart re-runs the same bundle and does not make any of these (documented or newly-found) pick up a merchant's runtime color — only a code change (making these reads reactive) or a rebuild with a new bundled default closes this gap. Given how many rounds of review have each surfaced another previously-uncounted consumer (`getTokenValue()` → `AppImage` → direct reads → `SecondaryButton` → `OpTouch` → `PrimaryButton` → `FormInput`/`colors` alias), this enumeration should be read as a growing sample of a systemic pattern — components reading the static `tokens.color` object or one of its aliases directly instead of Tamagui's reactive theme registry — not as a guaranteed-complete list; and a merchant-set 3-digit hex color (e.g. `#ABC`) is accepted by the backend/dashboard but silently rejected by mobile's 6-digit-only `isValidHexColor`, falling back to the bundled color with no error shown. All three documented in `docs/MOBILE_BRANDING_SPLIT_MATRIX.md`, though that doc explicitly does not track the `getTokenValue()`/`AppImage` reactivity gap's full scope (see its own "not tracked here" note) — this line is the accounting for that one.

Current state: Home modules/content are partial to implemented. The app uses `GET /customer/homescreen` generated hooks and renders backend-driven layout sections, but exact backend content availability depends on tenant data.

Current state: Product listing/detail is partial to implemented on the mobile side, but a 2026-07-03 cross-repo smoke test (`docs/CROSS_REPO_SMOKE_TEST.md`) found that at the tested backend commit the tsoa-generated routes these clients target — `/customer/search`, `/products/{id}`, plus `/cart/*`, `/checkout/*`, `/favorites` — failed to register at backend startup and returned 404, and the express `GET /products` listing 500ed. A fresh 2026-07-13 rerun against the only available local public backend candidate reached Railway, but every API probe returned `404 Application not found`, so catalog/search/cart/checkout runtime availability remains blocked on verified backend staging availability.

Current state: Cart/checkout is partial and high-risk. For private beta, the chosen mobile cart-to-checkout pipeline is the generated `/cart/*` Storefront cart surface plus generated `POST /checkout/handoff`, which returns a Shopify-hosted checkout URL. Add/update/remove/recovery in `useCartManager` stay on the generated cart client so the cart ID is a Shopify Storefront cart ID that handoff can use. The hand-written `/unified-cart` surface remains available for its existing local guest/customer cart uses, but it is not the primary cart-to-checkout surface because its real response is a local product cart (`status`, `data.cart`, `itemCount`) and does not expose a Shopify cart ID for hosted checkout. A 2026-07-13 smoke rerun attempted generated cart create/update/remove and checkout handoff against the only available local public backend candidate, but all returned Railway `404 Application not found`; no Shopify-hosted checkout URL was produced. Native checkout/payment changes remain high-risk and require human review.

Current state: Customer account/auth is partial to implemented. Login, signup, password reset, profile, token refresh, guest session, and persisted auth state exist. Auth refresh changes are high-risk.

Current state: Native app identity/builds are partial. Dynamic `app.config.ts` (env-driven identity; static `app.json` removed), checked-in iOS/Android projects, Firebase files, EAS config, and native plugins exist. Merchant identity resolution was verified through `npx expo config` and `npx expo prebuild` with a sample merchant (2026-07-02), and re-verified through `npx expo config --type public --json` on 2026-07-08, 2026-07-09, and 2026-07-13. As of 2026-07-03 (GitHub issue #60), `.easignore` excludes the checked-in native projects from EAS build archives, so EAS builds regenerate native projects from `app.config.ts` and can no longer silently ship Cartaisy identity; local `expo run:*` still uses the checked-in projects. As of 2026-07-13 (GitHub issue #89), `.gitignore` and `.easignore` also exclude generated local status/report artifacts (`/output/` and `cartaisy-*-status-report-*`) so EAS archive uploads do not include those untracked reports. As of 2026-07-13 (GitHub issue #86), `eas.json` includes a non-secret `sample-merchant-development` profile pointing at the real internal sample EAS project `@rendernext/acme-outfitters` (`a9a1dd2e-adfe-4020-9f10-6b561859f119`). No EAS or signed merchant build artifact has been produced because the sample project's Android remote credentials/keystore are not initialized for non-interactive builds.

Current state: Environment variable/secrets safety is partial to strong. `MOBILE_ENV_VARIABLES.md` documents safe/forbidden values, warns that `EXPO_PUBLIC_*` values ship to devices, and states that Shopify tokens belong on the backend only. Legacy public Shopify env keys are forbidden in local/sample mobile env guidance, and `mobileConfig` validates required public values and known forbidden public Shopify/secret-looking keys. If a real Shopify token was ever used in a public mobile env value, operators should rotate it without copying the token into docs, commits, issues, or logs.

Current state: Testing/CI is partial. Lint, TypeScript, and Jest scripts exist, with Jest coverage now spanning pure logic modules, auth refresh interceptors, cart/auth store state, and focused component/screen behavior. CI (`.github/workflows/ci.yml`) runs `npm ci`, typecheck, lint, and tests on pull requests against `main`. CI verification run (GitHub issue #94, 2026-07-17): all four steps green on a fresh PR (#95); Jest reported 15 suites / 79 tests. PR CI run: https://github.com/daneylpasha/Cartaisy/actions/runs/29578498115.

Current state: Release readiness is partial. A branded build checklist exists, but release readiness still depends on merchant identity, native assets, Firebase config, store-scoped backend setup, validation, and store submission checks.

## Target State

Target state: Mobile SaaS readiness means each merchant-branded build has verified public app configuration, native identity, Firebase/native services, backend tenant scoping, public runtime store config, safe branding, and validated checkout/payment behavior.

Target state: Runtime Shopify/catalog/store data should come from the Cartaisy backend, with Shopify Admin/private credentials kept off device.

Target state: Runtime branding (colors + logo) is implemented (PRs #115/#116); remaining work — the `companyName` string, the static-`tokens.color` reactivity gap (a growing, not-yet-exhaustively-enumerated set of consumers: `getTokenValue()`, `AppImage`, direct `tokens.color.primary`/`secondary` reads, the `assets/colors.ts` alias, and `SecondaryButton`'s, `OpTouch`'s, `PrimaryButton`'s, and `FormInput`'s own resolvers/defaults — see `docs/STATUS.md`'s current-state line for the full, still-growing accounting), the 3-digit-hex rejection, and a recorded end-to-end integration run — should land through explicit follow-up issues, not ad hoc changes. Dynamic app config is implemented; its per-merchant build output should be validated before branded releases.

Target state: Testing should include lint, typecheck, meaningful unit/integration coverage where available, platform smoke testing, and release-specific iOS/Android validation.

## Known Gaps

Known gap: Dynamic app config is implemented via `app.config.ts` and verified through `npx expo config` and `npx expo prebuild` with a sample merchant env (2026-07-02, see `docs/MOBILE_BRANDED_BUILD_CHECKLIST.md`), with public config re-verified on 2026-07-08, 2026-07-09, and 2026-07-13. No EAS or signed merchant build artifact has been produced. The checked-in-native-projects bypass was resolved on 2026-07-03 via `.easignore` (GitHub issue #60). The 2026-07-13 Android EAS development build attempt reached the real internal sample project but was blocked because Android remote credentials/keystore are not initialized and new keystore generation is unsupported in `--non-interactive` mode. Remaining blockers: initialize sample Android remote credentials through an approved secure flow, supply matching Firebase files through EAS file environment variables, rerun from a clean working tree, and install/verify the produced artifact.

Known gap: Runtime branding is implemented (colors + logo, PRs #115/#116), but three coverage gaps remain: the hardcoded `t("common.companyName")` string isn't wired to dashboard branding; `getTokenValue()` (~43 `$primary` + ~30 `$secondary` call sites across ~39 files, not a handful), `AppImage.resolveTokenColor()`, 66 further direct `tokens.color.primary`/`tokens.color.secondary` reads across 13 production files, `SecondaryButton`'s own local static-token resolver (used by 19 other files), `OpTouch`'s identical resolver (fed `backgroundColor="primary"` by 3 merchant-visible buttons directly, plus 35 of ~38 `PrimaryButton` instances via its own `"primary"` default), and `FormInput`'s `selectionColor={colors.primary}` (34 instances across 16 files, via the `assets/colors.ts` alias of `tokens.color`) don't reactively pick up a runtime color change, restart or not — and given how many review rounds have each found another uncounted consumer, this list should be treated as a representative sample of a systemic pattern, not a certified-complete inventory; and mobile's 6-digit-only `isValidHexColor` silently rejects a merchant-set 3-digit hex color that the backend/dashboard both accept. See `docs/MOBILE_BRANDING_SPLIT_MATRIX.md` for the split-matrix accounting and the 3-digit-hex discrepancy record — note the matrix itself does not track the `getTokenValue()`/`AppImage` reactivity gap's full scope. No live dashboard-edit → backend → mobile-app integration run has been recorded end-to-end; each stage's evidence today is component-level (backend contract, mobile hooks, dashboard editor tested independently), not a single connected trace.

Known gap: Native identity, Firebase, Apple Pay, and push notification mismatches identified in audits need dedicated release or implementation work.

Known gap: Jest coverage includes pure logic modules, API client auth refresh interceptors, cart/auth store state, and focused component/screen suites, but it remains incomplete. There is no E2E setup, no device build CI, no payment success/failure E2E coverage, and no full navigation coverage; broader screen behavior and checkout step internals still need dedicated tests.

Known gap: CI and build readiness should be verified before release; do not assume this exists unless verified in code and pipeline results.

## Unknowns

Unknown: Deployed backend availability. As of 2026-07-13, GitHub issue #87 did not provide a new verified staging API URL or safe Store A / Store B IDs, and the currently configured Railway backend `https://cartaisy-backend-production.up.railway.app/api/v1` still returns platform-level `404 Application not found`; no deployed backend sandbox is reachable from the mobile repo configuration. The 2026-07-03 cross-repo smoke test (`docs/CROSS_REPO_SMOKE_TEST.md`) proved spec-level sync does not imply runtime availability: the backend commit tested there failed to register its tsoa routes at startup, so the spec-described search/product-detail/cart/checkout endpoints all 404 while express-only routes (store config, homescreen, unified cart, orders) worked. As of GitHub issue #72, the mobile spec snapshot has been refreshed to include backend `POST /checkout/handoff`; issue #87 re-ran the smoke suites, but runtime validation still needs a reachable sandbox with store-scoped Shopify Storefront credentials.

Unknown: Current backend tenant-mismatch behavior. GitHub issue #87 attempted fresh tenant-mismatch verification on 2026-07-13, but without a verified reachable staging URL and safe Store A / Store B IDs, the run could only confirm that the configured Railway target is not serving the Cartaisy app. Treat tenant-mismatch behavior as unverified for the current backend until a seeded sandbox with at least two test stores is available.

Unknown: Whether real EAS/signed merchant builds succeed end-to-end (signing, store submission, on-device identity). Config evaluation and local prebuild output were verified with a sample merchant on 2026-07-02, config output was re-verified on 2026-07-08, 2026-07-09, and 2026-07-13, and Android EAS development build commands were attempted on 2026-07-08, 2026-07-09, and 2026-07-13. The latest attempt reached the real internal sample EAS project but stopped at non-interactive Android keystore generation. No installable artifact exists yet.

## Current Priority Areas

- Backend/mobile runtime validation: provision or identify a verified reachable backend staging/sandbox URL with safe Store A / optional Store B IDs and store-scoped Shopify Storefront credentials, then re-run the cart-to-hosted-checkout smoke path so generated `/cart/*` and `/checkout/handoff` are verified beyond spec sync.
- Keep mobile secrets safety strict, especially around Shopify Admin/private credentials.
- Preserve backend-owned tenant isolation and store-scoped API usage.
- Resolve native identity/build mismatches before production branded releases.
- Runtime branding (colors + logo) is implemented; remaining branding work is scoped follow-up issues — wire the `companyName` string, make all static-`tokens.color` reads reactive (`getTokenValue()`/`AppImage`: ~73 call sites; plus 66 direct `tokens.color.primary`/`secondary` reads across 13 files, `SecondaryButton`'s own resolver, `OpTouch`'s own resolver, `PrimaryButton`'s default (35 of ~38 instances, spanning checkout/auth/address/product actions), and `FormInput`'s `colors.primary` alias read (34 instances across 16 files) — not a handful, and likely still not a complete list; a code-level audit, not further doc inventory, is what actually closes this), accept 3-digit hex on mobile to match backend/dashboard, and record a live dashboard→backend→mobile-app integration run.
- Prove merchant-branded build output end-to-end.
- Extend Jest coverage when backend/mobile behavior changes.

## Related Docs And Issues

- `CARTAISY_CONTEXT.md`
- `docs/ARCHITECTURE.md`
- `docs/MOBILE_BRANDED_BUILD_CHECKLIST.md`
- `docs/MOBILE_ENV_VARIABLES.md`
- `docs/MOBILE_BRANDING_CONFIG_AUDIT.md`
- `docs/DYNAMIC_APP_CONFIG_MIGRATION_AUDIT.md`
- `docs/MOBILE_RUNTIME_BRANDING_CONTRACT.md`
- `docs/MOBILE_MERCHANT_PROVISIONING_RUNBOOK.md`
- `docs/IOS_PROVISIONING_SCOPE.md`
- GitHub issue #40
