/**
 * Shopper-facing line shown beside Cart checkout and product Buy Now.
 * Checkout itself still opens the Shopify-hosted URL from
 * POST /checkout/handoff. This copy only sets that expectation.
 */
export const HOSTED_CHECKOUT_NOTE =
  "Secure checkout continues on the store's page.";
