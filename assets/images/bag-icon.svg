import * as React from "react";
import Svg, { Path } from "react-native-svg";
import type { SvgProps } from "react-native-svg";

const BagIcon = (props: SvgProps) => (
  <Svg
    width={24}
    height={24}
    viewBox="0 0 24 24"
    fill="none"
    {...props}
  >
    <Path
      d="M6 6h12v13a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2V6Z"
      stroke={props.color || props.fill || "#000"}
      strokeWidth={2}
      strokeLinecap="round"
      strokeLinejoin="round"
      fill="none"
    />
    <Path
      d="M9 6V5a3 3 0 0 1 3-3v0a3 3 0 0 1 3 3v1"
      stroke={props.color || props.fill || "#000"}
      strokeWidth={2}
      strokeLinecap="round"
      strokeLinejoin="round"
      fill="none"
    />
    <Path
      d="M6 9h12"
      stroke={props.color || props.fill || "#000"}
      strokeWidth={2}
      strokeLinecap="round"
      strokeLinejoin="round"
    />
  </Svg>
);

export default BagIcon;
